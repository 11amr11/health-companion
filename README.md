You can open the website through here => https://11amr11.github.io/health-companion

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `API_KEY` in [.env.local](.env.local) to your (-----) API key
3. Run the app:
   `npm run dev`
